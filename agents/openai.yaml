interface:
  display_name: "FameEX Figma to Code"
  short_description: "Make targeted FameEX UI changes or strict Figma implementations"
  default_prompt: "Use $fameex-figma-to-code for a targeted change by default, strict visual parity when explicitly required, or a full product feature when the scope spans requirements and routes."
